#include "unroll.h"

int unroll(Btor2Parser *bfr, int unroll_time)
{
    // get `state` init value
    // remove `state`

    // sawp `state` in ops with init_value
    // save `next` as new_value at next clock

    for (int i = 1; i <= unroll_time; i++)
    {
        // copy one time
        // sawp init_value in ops with new_value
        // save `next` as new_value at next clock
    }

    // relocate all lines
    return 0;
}